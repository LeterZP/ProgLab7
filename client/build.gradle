group = 'com.leterzp.prog.client'
version = '3.0'

dependencies {
    implementation project(":general")
}

apply plugin: 'com.gradleup.shadow'

shadowJar {
    archiveBaseName.set('ProgLab7.client')
    archiveVersion.set('3.0')
    manifest {
        attributes 'Main-Class': 'ClientKt'
    }
}